# Cerina Protocol Foundry — MCP Tool Manifest

## Tool Name
cerina_protocol_generator

## Description
Generates safe, human-reviewable clinical protocols using a
multi-agent LangGraph workflow with persistent checkpointing
and human-in-the-loop approval.

---

## Input Schema
```json
{
  "prompt": "string — free-text protocol request"
}
